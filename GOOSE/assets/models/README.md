# Model Weights Directory

Place your custom trained YOLO model files here.

### Required Filenames:
- `targetModel.onnx` (Recommended for performance)
- `targetModel.pt` (Fallback)

The application will automatically prioritize the `.onnx` file if both are present.